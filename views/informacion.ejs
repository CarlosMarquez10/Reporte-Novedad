<!DOCTYPE html>
<html lang="es">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Lexend:wght@100..900&display=swap"
      rel="stylesheet"
    />
    <title>Información de Novedades Revisadas</title>
    <link rel="stylesheet" href="/css/styles.css" />
    <style>
      main {
        width: 100%;
        display: flex;
        flex-direction: column;
        justify-content: flex-start;
        align-self: start;
      }

      .container {
        display: flex;
        flex-wrap: wrap;
        justify-content: space-around;
      }

      .card {
        background-color: #fff;
        border-radius: 8px;
        box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
        margin: 20px;
        padding: 20px;
        width: 300px;
        box-sizing: border-box;
      }

      .card img {
        width: 100%;
        height: 250px;
        object-fit: cover;
        border-radius: 8px;
      }

      .card h2 {
        font-size: 1.5em;
        margin: 10px 0;
      }

      .card p {
        font-size: 1em;
        margin: 5px 0;
      }

      .card-buttons {
        display: flex;
        gap: 10px;
        margin-top: 10px;
      }

      .card-buttons form {
        flex-grow: 1;
      }

      .card-buttons button {
        width: 100%;
        padding: 10px;
        border: none;
        border-radius: 4px;
        cursor: pointer;
        font-size: 16px;
      }

      .card-buttons button.ver-button {
        background-color: #007bff;
        color: white;
      }

      .card-buttons button.ver-button:hover {
        background-color: #0056b3;
      }

      .navbar {
        display: flex;
        justify-content: space-between;
        align-items: center;
        background-color: #343a40;
        padding: 10px 20px;
        color: white;
      }

      .navbar img {
        height: 40px;
      }

      .navbar .menu {
        display: flex;
        gap: 20px;
      }

      .navbar .menu a {
        color: white;
        text-decoration: none;
        font-size: 16px;
        padding: 5px 10px;
      }

      .navbar .menu a:hover {
        background-color: #495057;
        border-radius: 5px;
      }

      .textAreaObs {
        width: 100%;
        height: 80px;
        box-sizing: border-box;
        padding: 10px;
        font-size: 16px;
        overflow: hidden;
        text-overflow: ellipsis;
        white-space: nowrap;
        display: -webkit-box;
        -webkit-line-clamp: 3;
        -webkit-box-orient: vertical;
      }
    </style>
  </head>
  <body>
    <!-- Incluir la barra de navegación -->
    <%- include('components/navbar', { user: user }) %>

    <main>
      <h1>Información de Novedades Revisadas</h1>

      <!-- Filtros -->
      <form id="filterForm" action="/informacion" method="GET">
        <label for="fecha">Filtrar por Fecha:</label>
        <input type="date" id="fecha" name="fecha" />

        <label for="regional">Filtrar por Regional:</label>
        <select id="regional" name="regional">
          <option value="">Todas</option>
          <option value="CUCUTA">CUCUTA</option>
          <option value="PAMPLONA">PAMPLONA</option>
          <option value="OCAÑA">OCAÑA</option>
          <option value="TIBU">TIBU</option>
          <option value="TTIBU-PUEBLOS">TTIBU-PUEBLOS</option>
          <option value="AGUACHICA">AGUACHICA</option>
        </select>

        <button type="submit">Aplicar Filtros</button>
      </form>

      <div class="container">
        <% novedades.forEach(novedad => { %>
        <div class="card">
          <img src="/<%= novedad.fotoPath %>" alt="Foto de la novedad" />
          <h2><%= novedad.nombre %></h2>
          <p><strong>Fecha de Novedad:</strong> <%= novedad.terminal %></p>
          <p><strong>Regional:</strong> <%= novedad.regional %></p>
          <textarea class="textAreaObs" readonly>
<%= novedad.observacion %></textarea>
          <div class="card-buttons">
            <form action="/ver/<%= novedad.id %>" method="GET">
              <button type="submit" class="ver-button">Ver</button>
            </form>
          </div>
        </div>
        <% }); %>
      </div>
    </main>
  </body>
</html>
